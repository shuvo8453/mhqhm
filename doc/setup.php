<?php

// git clone https://github.com/joy2362/cms

// copy .env to project directory (env file present in doc folder)

// change app_url , app_name and other necessary config in .env file

// composer install (install required dependency)

// php artisan key:generate (change app_key)

// create db same as env database name or change database name with your own (config database)

// php artisan migrate:fresh --seed (migrate database with pre defined value)

// npm install

// npm run dev

// visit your local env

// go to '/admin/system-update'

// go to /cache/clear

// done

// everything work right?
